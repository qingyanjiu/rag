# Qwen3.5 微调项目依赖
# 安装命令: uv pip install -r requirements.txt

# Unsloth - 高效微调框架（自动安装适配的 torch 版本）
# 注意: 如果已安装 torch，需先卸载再安装 unsloth 以防版本冲突
unsloth>=2025.3.0

# Hugging Face 生态
transformers>=4.48.0
datasets>=3.0.0
accelerate>=1.3.0
huggingface-hub>=0.28.0

# 训练监控
wandb>=0.19.0

# 数据预处理
sentencepiece>=0.2.0

# 配置
pyyaml>=6.0

# tokenizer 加速
tokenizers>=0.21.0
